package com.api2api.ohs.http.admin.dto;

/**
 * Admin change user status request context (no request body, only path variable + operator from header).
 */
public class AdminChangeUserStatusRequest {
    // This class exists for documentation purposes.
    // Actual targetUserId from path variable, operatorUserId from CurrentUserContextResolver.
}

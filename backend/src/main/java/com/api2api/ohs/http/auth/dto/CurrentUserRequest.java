package com.api2api.ohs.http.auth.dto;

/**
 * Current user request context (no request body, only resolved from header).
 */
public class CurrentUserRequest {
    // This class exists for documentation purposes.
    // Actual currentUserId is resolved from CurrentUserContextResolver.
}
